<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <OutputType>Exe</OutputType>
    <RootNamespace>FieldCure.Mcp.Rag</RootNamespace>
    <AssemblyName>fieldcure-mcp-rag</AssemblyName>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>

    <Version>2.5.0</Version>
    <PackageId>FieldCure.Mcp.Rag</PackageId>

    <Authors>Jong Hyun Kim</Authors>
    <Company>Fieldcure Co., Ltd.</Company>
    <Copyright>Copyright (c) 2026 Fieldcure Co., Ltd.</Copyright>
    <Description>MCP RAG server with hybrid BM25 + vector search and AI-powered chunk contextualization. Chunks documents, enriches chunks with AI-generated context and keywords, generates embeddings, and performs keyword (FTS5) and semantic (cosine similarity) search with Reciprocal Rank Fusion.</Description>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageProjectUrl>https://github.com/fieldcure/fieldcure-mcp-rag</PackageProjectUrl>
    <RepositoryUrl>https://github.com/fieldcure/fieldcure-mcp-rag</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageTags>mcp;mcp-server;ai;dotnet;rag;embedding;vector-search;knowledge-base;bm25</PackageTags>
    <PackageReleaseNotes>v2.5.0: ARM64 dnx support + Audio 0.4 effective-model reporting. Picks up FieldCure.DocumentParsers.Ocr 1.2.0 which adds the multi-arch tool layout (tools/&lt;tfm&gt;/any/x64/ + tools/&lt;tfm&gt;/any/arm64-platform/x64/) and a runtime CustomSearchPath router, so this tool nupkg now installs and runs cleanly under dnx on both win-x64 and win-arm64 from a single package. Picks up FieldCure.DocumentParsers.Audio 0.4 and has LazyAudioTranscriber implement the new IModelSizeReporting capability — the rendered transcript header now reflects the effective model size that was actually used (e.g., "| Model | large |") instead of the caller-requested size we were reporting through 2.4.4. Chunk-level audio metadata (audio.model_size in the chunk JSON) is unchanged — the fix is the in-transcript header only. Adds a smoke-ocr diagnostic CLI mode (fieldcure-mcp-rag smoke-ocr --pdf &lt;scanned.pdf&gt;) used by the new manual arm64-dnx-smoke.yml workflow to validate the cross-arch deployment path on a windows-11-arm runner. The OCR PackageReference Version is now parameterizable via the OcrPackageVersion MSBuild property (default "1.*") so future preview pins can be supplied without csproj edits.</PackageReleaseNotes>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <PackageIcon>Logo.png</PackageIcon>

    <PackageType>McpServer</PackageType>
    <PackAsTool>true</PackAsTool>
    <ToolCommandName>fieldcure-mcp-rag</ToolCommandName>

    <!-- OCR package version. Default floats to the latest stable 1.x. The
         ARM64 dnx smoke workflow (.github/workflows/arm64-dnx-smoke.yml)
         overrides this with /p:OcrPackageVersion=[1.2.0-preview.1] to pin
         to a specific prerelease while validating the multi-arch
         PackAsTool path before stable 1.2.0 is published. -->
    <OcrPackageVersion Condition="'$(OcrPackageVersion)' == ''">1.*</OcrPackageVersion>
  </PropertyGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="FieldCure.Mcp.Rag.Tests" />
  </ItemGroup>

  <ItemGroup>
    <None Include="README.md" Pack="true" PackagePath="" />
    <None Include="Logo.png" Pack="true" PackagePath="" />
    <None Include=".mcp/server.json" Pack="true" PackagePath=".mcp/" />
  </ItemGroup>

  <!-- Sync check: .mcp/server.json's "version" fields must match csproj <Version>.
       The JSON descriptor is metadata for the MCP Server Registry; if the two
       drift, the registry listing reports the wrong version even though the
       published nupkg itself is correct. Caught this on 2.5.0 (server.json had
       stayed at 2.4.4) and added the check so the next pack fails early on a
       missed update. Hits both top-level "version" and packages[].version
       because we look for the literal token "$(Version)" — both fields use
       the same csproj <Version> value by convention. -->
  <Target Name="VerifyServerJsonVersion" BeforeTargets="GenerateNuspec">
    <PropertyGroup>
      <_ServerJsonContent>$([System.IO.File]::ReadAllText('$(MSBuildProjectDirectory)\.mcp\server.json'))</_ServerJsonContent>
      <_ExpectedVersionToken>"version": "$(Version)"</_ExpectedVersionToken>
    </PropertyGroup>
    <Error Condition="!$(_ServerJsonContent.Contains($(_ExpectedVersionToken)))"
           Text=".mcp/server.json version is out of sync with csproj &lt;Version&gt;$(Version)&lt;/Version&gt;. Update both 'version' fields in .mcp/server.json to match before packing." />
  </Target>

  <ItemGroup>
    <PackageReference Include="ModelContextProtocol" Version="1.2.0" />
    <PackageReference Include="Microsoft.Extensions.Hosting" Version="8.*" />
    <PackageReference Include="Microsoft.Data.Sqlite" Version="8.*" />
    <PackageReference Include="System.Net.Http.Json" Version="8.*" />
    <PackageReference Include="FieldCure.DocumentParsers" Version="2.*" />
  </ItemGroup>

  <!-- OCR is Windows-only (Tesseract native binaries). On Linux/macOS the core
       PdfPig text-only parser is used; scanned-PDF pages yield empty text.
       Audio is Windows-only too (Whisper.net + NAudio Media Foundation): on
       non-Windows the audio extensions are unsupported and matching files
       silently return empty text from DocumentParserFactory.GetParser. -->
  <ItemGroup Condition="$([MSBuild]::IsOSPlatform('Windows'))">
    <PackageReference Include="FieldCure.DocumentParsers.Ocr" Version="$(OcrPackageVersion)" />
    <PackageReference Include="FieldCure.DocumentParsers.Audio" Version="0.4.*" />

    <!-- Whisper.net CPU native runtime does not flow transitively into the
         packaged dotnet-tool output: PackAsTool strips runtimes/<rid>/native/
         from indirect dependencies, so without a direct PackageReference here
         the published tool would throw "Native Library not found in default
         paths" on the first audio indexing call.

         As of DocumentParsers.Audio v0.3 this is the *only* reason the
         reference is restated. The GPU runtimes (Whisper.net.Runtime.Cuda,
         Whisper.net.Runtime.Vulkan) are no longer in the dependency graph at
         all — Audio v0.3 dropped them and instead downloads them on first GPU
         use from https://github.com/fieldcure/fieldcure-whisper-runtimes
         (cached under %LOCALAPPDATA%\FieldCure\WhisperRuntimes\). The 250 MB
         nuget.org cap that drove the earlier CPU-only trade-off is no longer
         a constraint here: only the CPU runtime ever lands inside this nupkg. -->
    <PackageReference Include="Whisper.net.Runtime" Version="1.9.0" />
  </ItemGroup>

  <PropertyGroup Condition="$([MSBuild]::IsOSPlatform('Windows'))">
    <DefineConstants>$(DefineConstants);WINDOWS_OCR;WINDOWS_AUDIO</DefineConstants>
  </PropertyGroup>

  <ItemGroup Condition="!$([MSBuild]::IsOSPlatform('Windows'))">
    <Compile Remove="LazyOcrEngine.cs" />
    <Compile Remove="LazyAudioTranscriber.cs" />
  </ItemGroup>

</Project>
